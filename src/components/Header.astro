---
import { languages, type Lang } from '../i18n/ui';
import { useTranslations } from '../i18n/utils';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);

const otherLang: Lang = lang === 'en' ? 'pt' : 'en';
const BASE_URL = import.meta.env.MODE === 'production' ? '/portfolio-dev' : '';

const navLinks = [
  { href: '#about', label: t('nav.about') },
  { href: '#experience', label: t('nav.experience') },
  { href: '#projects', label: t('nav.projects') },
  { href: '#contact', label: t('nav.contact') },
];
---

<header class="sticky top-0 z-20 border-b border-ink-700/60 bg-ink-950/85 backdrop-saturate-150 supports-[backdrop-filter]:backdrop-blur-sm">
	<div class="flex items-center justify-between px-6 py-4">
		<a href={`${BASE_URL}/${lang}/`} class="font-display text-sm font-semibold tracking-tight text-paper">
			Felipe Trombini
		</a>

		<nav class="hidden items-center gap-1 md:flex">
			{navLinks.map(({ href, label }) => (
				<a
					href={href}
					class="rounded-full px-3 py-1.5 font-mono text-xs uppercase tracking-wider text-paper-muted transition-colors hover:bg-ink-800 hover:text-paper"
				>
					{label}
				</a>
			))}
			<a
				href={`${BASE_URL}/${otherLang}/`}
				class="ml-2 rounded-full border border-ink-600 px-3 py-1.5 font-mono text-xs uppercase tracking-wider text-paper-muted transition-colors hover:border-copper-500 hover:text-copper-400"
			>
				{languages[otherLang]}
			</a>
		</nav>

		<details class="relative md:hidden">
			<summary
				class="cursor-pointer list-none rounded-full border border-ink-600 px-3 py-1.5 font-mono text-xs uppercase tracking-wider text-paper-muted"
			>
				Menu
			</summary>
			<div class="absolute right-0 top-full z-10 mt-2 w-52 rounded-2xl border border-ink-700 bg-ink-800 p-2 shadow-xl shadow-black/40">
				{navLinks.map(({ href, label }) => (
					<a
						href={href}
						class="block rounded-xl px-4 py-2 font-mono text-xs uppercase tracking-wider text-paper-muted hover:bg-ink-700 hover:text-paper"
					>
						{label}
					</a>
				))}
				<a
					href={`${BASE_URL}/${otherLang}/`}
					class="block rounded-xl px-4 py-2 font-mono text-xs uppercase tracking-wider text-copper-400 hover:bg-ink-700"
				>
					{languages[otherLang]}
				</a>
			</div>
		</details>
	</div>
</header>
